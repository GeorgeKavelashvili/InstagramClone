import React, { useState, useEffect } from "react";
import { View, Text, FlatList } from "react-native";

const ActivityScreen = ({ videoId }) => {
  const [activity, setActivity] = useState([]);

  return (
    <View>
      <Text>daikbn</Text>
    </View>
  );
};

export default ActivityScreen;
